{{/* Unified robots.txt across all sites.
		 Uses optional site param overrides:
			 [params.robots]
				 disallowAll = true    # forces Disallow: /
				 extra = ["Disallow: /private/", "Allow: /"]
*/}}
User-agent: *
{{- $cfg := site.Params.robots -}}
{{- if and $cfg (isset $cfg "disallowAll") $cfg.disallowAll -}}
Disallow: /
{{- else -}}
Allow: /
{{- end }}
Sitemap: {{ (print site.BaseURL "sitemap.xml") | replace "//sitemap" "/sitemap" }}
{{- if and $cfg (isset $cfg "extra") }}
{{- range $cfg.extra }}
{{ . }}
{{- end }}
{{- end }}
