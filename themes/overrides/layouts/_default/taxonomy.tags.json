{{- /* build a slice of `{id, name}` objects for each tag */ -}}
{{- $tags := slice -}}
{{- range $name := sort (keys .Site.Taxonomies.tags) -}}
  {{- $tags = $tags | append (dict "id" $name "name" $name) -}}
{{- end -}}
{{- /* serialize that as JSON */ -}}
{{- jsonify $tags -}}
