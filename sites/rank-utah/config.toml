baseURL       = "https://clearpresence.io/"
# use | to add a second line
title         = "Clear Presence"
languageCode  = "en-us"
themesDir     = "../../themes"
 # noop: 2025-11-25 ensure shared robots.txt in use

trailingSlash = "never"
cleanDestinationDir = true
uglyURLs      = false

[module]

  # Ensure the site's own content directory is mounted (required when using module mounts)
  [[module.mounts]]
    source = "content"
    target = "content"

  [[module.mounts]]
    source = "static"
    target = "static"

  [[module.mounts]]
    source = "media"
    target = "assets/media"

  [[module.mounts]]
    source = "media"
    target = "static/media"

  # Ensure posts are included as site content
  [[module.mounts]]
    source = "posts"
    target = "content/blog"

# Flowbite layout cascade is provided by shared config


[outputs]
  # Inherit defaults; override only if needed
  home         = ["HTML", "RSS", "JSON", "FORMS"]

[params]
  env = "production"
  primaryColor        = "blue"
  logo = "media/logo.png"
  lightBackground  = "gray-200"  # any color @ 50|100|200
  darkBackground   = "gray-900" # neutral @ 800|900|950
  cardBackground     = "gray-100"     # or "slate-100", "rose-50", etc.
  cardBackgroundDark = "gray-800"  # or "slate-800", "black", etc.
  cardBorderLight    = "gray-200"  # no "border-" prefix
  cardBorderDark     = "gray-700"  # no "border-" prefix

  showReadingTime     = true
  enableSearch        = true
  clientName          = "Clear Presence"
  clientURL           = "https://clearpresence.io"
  mainSections        = ["posts"]
  # defaultTheme/disableThemeToggle centralized in shared config
  author              = "Benjamin Awerkamp"
  hideMeta            = false

[params.schema.organization]
enabled = true
name = "Clear Presence LLC"
legalName = "Clear Presence LLC"
url = "https://clearpresence.io/"
logo = "/media/logo.png"
sameAs = []


[params.socialDefaults]
  size = "w-8 h-8"
  gap  = "gap-3"

[params.analytics]
  # Load analytics immediately (no delay/gating)
  delayMs = 0
  # Temporary: enable GA4 debug_mode for Tag Assistant / DebugView visibility
  debugMode = true

  [params.analytics.google]
    id = "G-7240SPB67L"
    enabled = true

  [params.analytics.clarity]
    id = "u9tbqd2kqk"
    enabled = true

  [params.analytics.googleAds]
    id = "AW-XXXXXXXXX"   # replace with your Google Ads ID
    enabled = false        # set true when ready

  [params.analytics.meta]
    id = ""               # e.g., 123456789012345 (Meta Pixel)
    enabled = false        # set true when ready

[params.header]
  stickyMobile     = true
  stickyDesktop    = true
  # one of: sm | base | lg | xl | 2xl | 3xl | 4xl
  titleSize = "2xl"
  showCTAOnMobile  = false
  showCTAOnDesktop = true
  CTAText          = "Contact"
  CTAURL           = "/contact"
  # CTAClasses       = "text-white bg-rose-600 hover:bg-rose-700 focus:ring-rose-300"
  showPhoneOnMobile = true
  phoneNumber       = "385-323-8130"
  logoHeight        = "h-10"
  # Optional: explicit pixel dimensions to avoid layout shift on logo
  # Set these to your actual logo intrinsic size
  logoWidth         = 0
  logoHeightPx      = 40
  theme = "blend" #blend (default), invert, glass

[params.forms]
  action         = "https://submit-form.com/DOH188lWF"
  honeypot       = ""                   # disabled (Turnstile in place)
  successMessage = "Thanks! Your message was sent."
  errorMessage   = "Sorry, something went wrong. Please try again."
  redirect       = "https://clearpresence.io/thank-you"
  # submitClasses = "..."               # optional button styling

[params.turnstile]
  # Enable Cloudflare Turnstile on forms rendered via the contact-form shortcode
  enable  = true
  # Reuse Blue Ridge Abbey site key (make sure blueridgetech.com and rankutah domains are allowed in CF)
  sitekey = "0x4AAAAAAB8Xlhe4_-six2GL"

[params.section.defaults]
  # Container width (sitewide content width):
  #   sm → max-w-screen-sm
  #   md → max-w-3xl
  #   lg → max-w-5xl
  #   xl → max-w-7xl
  #   2xl → max-w-screen-2xl
  #   full → max-w-none (edge to edge)
  max   = "xl"

  # Horizontal padding (sitewide gutters):
  #   none → px-0, sm → px-4, md → px-6, lg → px-8
  # Tip: keep a gutter even on full-width for mobile readability.
  pad   = "lg"

  # Note: Vertical page-edge padding and per-section alignment were removed.
  # Spacing inside content uses default typography; no extra sitewide controls.


# [params.button_outline]

[permalinks]
  blog = "/blog/:contentbasename"

[taxonomies]
  tag = "tags"

#[build]  # uses shared build settings from theme; uncomment to override

[menu]

  [[menu.main]]
    name = "Services"
    url = "/services/"
    identifier = "services"
    weight = 10

  [[menu.main]]
    name = "Website Design"
    url = "/services/website-design"
    parent = "services"
    weight = 11

  [[menu.main]]
    name = "Local SEO"
    url = "/services/local-seo"
    parent = "services"
    weight = 12

  [[menu.main]]
    name   = "Portfolio"
    url    = "/portfolio"
    weight = 20

  [[menu.main]]
    name   = "Pricing"
    url    = "/pricing"
    weight = 30

  [[menu.main]]
    name   = "Blog"
    url    = "/blog/"
    weight = 35


  [[menu.main]]
    name = "Areas"
    identifier = "areas"
    weight = 40

  [[menu.main]]
    name = "Pleasant Grove, UT"
    url = "/locations/pleasant-grove-ut"
    parent = "areas"
    weight = 41

  [[menu.main]]
    name = "Boca Raton, FL"
    url = "/locations/boca-raton-fl"
    parent = "areas"
    weight = 42

  [[menu.main]]
    name = "Grand Junction, CO"
    url = "/locations/grand-junction-co"
    parent = "areas"
    weight = 43

  [[menu.main]]
    name = "Buena Vista, VA"
    url = "/locations/buena-vista-va"
    parent = "areas"
    weight = 44

