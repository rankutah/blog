baseURL       = "https://clearpresence.io/"
# use | to add a second line
title         = "Clear Presence"
languageCode  = "en-us"
theme         = ["overrides", "PaperMod"]
themesDir     = "../../themes"

# Remove trailing slashes from generated URLs
trailingSlash = "never"
cleanDestinationDir = true
uglyURLs      = true
# Ensure FORMS format is defined locally so outputs.home can use it reliably
[outputFormats.FORMS]
  mediaType = "application/json"
  baseName = "forms"
  isPlainText = true
  path = ".well-known"

[module]


[outputs]
  # Inherit defaults; override only if needed
  home         = ["HTML", "RSS", "JSON", "FORMS"]

[params]
  googleAnalyticsID   = "G-LGYK5V1CTV"
  primaryColor        = "indigo"
  lightBackground  = "gray-50"  # any color @ 50|100|200
  darkBackground   = "gray-900" # neutral @ 800|900|950
  cardBackground     = "gray-100"     # or "slate-100", "rose-50", etc.
  cardBackgroundDark = "gray-800"  # or "slate-800", "black", etc.
  cardBorderLight    = "gray-200"  # no "border-" prefix
  cardBorderDark     = "gray-700"  # no "border-" prefix



  showReadingTime     = true
  enableSearch        = true
  clientName          = "Clear Presence Studio"
  clientURL           = "https://rankutah.com"
  mainSections        = ["posts"]
  defaultTheme        = "auto"
  disableThemeToggle  = false
  author              = "Benjamin Awerkamp"
  hideMeta            = false

[params.analytics]
  delayMs = 4000
  loadEvents = ["scroll", "mousemove", "touchstart"]

  [params.analytics.google]
    id = "G-LGYK5V1CTV"
    enabled = true

  [params.analytics.clarity]
    id = "orwqd1fgew"
    enabled = true

  [params.analytics.googleAds]
    id = "AW-XXXXXXXXX"   # replace with your Google Ads ID
    enabled = false        # set true when ready

  [params.analytics.meta]
    id = ""               # e.g., 123456789012345 (Meta Pixel)
    enabled = false        # set true when ready

[params.header]
  stickyMobile     = true
  stickyDesktop    = true
  # one of: sm | base | lg | xl | 2xl | 3xl | 4xl
  titleSize = "2xl"
  showCTAOnMobile  = false
  showCTAOnDesktop = true
  CTAText          = "Get a Quote"
  CTAURL           = "/contact/"
  # CTAClasses       = "text-white bg-rose-600 hover:bg-rose-700 focus:ring-rose-300"
  showPhoneOnMobile = true
  phoneNumber       = "385-323-8130"
  logoHeight        = "h-10"
  theme = "blend" #blend (default), invert, glass

[params.forms]
  action         = "https://submit-form.com/DOH188lWF"
  honeypot       = "_hp"                # must match Formspark
  successMessage = "Thanks! Your message was sent."
  errorMessage   = "Sorry, something went wrong. Please try again."
  # submitClasses = "..."               # optional button styling

[params.section.defaults]
  # Container width:
  #   sm → max-w-screen-sm
  #   md → max-w-3xl
  #   lg → max-w-5xl
  #   xl → max-w-7xl
  #   2xl → max-w-screen-2xl
  #   full → max-w-none (edge to edge)
  max   = "xl"

  # Horizontal padding:
  #   none → px-0, sm → px-4, md → px-6, lg → px-8
  pad   = "lg"

  # Vertical padding:
  #   none → py-0, sm → py-6, md → py-12, lg → py-16, xl → py-24
  y     = "none"

  # Text align: start | center
  align = "start"

  # Optional vertical centering:
  # alignY: "", "start", "center", "end", "between"
  # minh:   "", "screen", "full"
  # alignY = "center"
  # minh   = "screen"



#  [params.button]
#    classes = "text-white bg-stone-700 hover:bg-stone-800 focus:ring-4 focus:ring-stone-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-stone-600 dark:hover:bg-stone-700 focus:outline-none dark:focus:ring-stone-800"

# [params.button_outline]



[permalinks]
  posts = "/:contentbasename"

[taxonomies]
  tag = "tags"

#[build]  # uses shared build settings from theme; uncomment to override

[menu]

  [[menu.main]]
    name = "Services"
    url = "/services/"
    identifier = "services"
    weight = 10

  [[menu.main]]
    name = "Website Design"
    url = "/services/website-design"
    parent = "services"
    weight = 11

  [[menu.main]]
    name = "Local SEO"
    url = "/services/local-seo"
    parent = "services"
    weight = 12

  [[menu.main]]
    name   = "Portfolio"
    url    = "/portfolio"
    weight = 20

  [[menu.main]]
    name   = "Pricing"
    url    = "/pricing"
    weight = 30

  [[menu.main]]
    name   = "About"
    url    = "/about"
    weight = 40

  [[menu.main]]
    name   = "Contact"
    url    = "/contact"
    weight = 50
